using CoreGame;
using System.Collections.Generic;
using UnityEngine;

public class TimelineTestManager : MonoBehaviour
{
    public GameObject objectToInstanciate;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {
    }
}
